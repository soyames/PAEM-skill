# Conventions

- Route handlers never log or return password hashes
- All new routes get at least one test file before being marked complete in `completed_tasks.md`
- Commit messages describe the change, not the AI session that made it
